#!/bin/bash

set -e

export LDFLAGS="-Wl,-rpath=$ARTIFACT/lib -Wl,-rpath=$ZLIB/lib -Wl,-rpath=$BZIP2/lib -Wl,-rpath=$NCURSES/lib"
export CFLAGS="-I$ZLIB/include -I$BZIP2/include -I$NCURSES/include"
export CPPFLAGS="$CFLAGS"

# Fix for #21:
export HAS_HG="no"
./configure --prefix=$ARTIFACT --enable-shared
